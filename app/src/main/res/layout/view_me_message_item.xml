<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:padding="4dp">

    <de.hdodenhof.circleimageview.CircleImageView
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:src="@mipmap/head1"
        android:id="@+id/me_user_icon"
        app:layout_constraintEnd_toEndOf="parent"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/secondary_text"
        app:layout_constraintEnd_toStartOf="@id/me_user_icon"
        android:layout_marginEnd="8dp"
        android:textSize="12sp"
        android:id="@+id/me_user_name"
        />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintEnd_toStartOf="@id/me_user_icon"
        app:layout_constraintTop_toBottomOf="@id/me_user_name"
        android:textSize="16sp"
        android:textColor="#ffffff"
        android:background="@drawable/me_message_drawable"
        android:layout_marginEnd="8dp"
        android:layout_marginTop="8dp"
        android:id="@+id/me_text_data"
        android:maxWidth="280dp"
        android:padding="8dp"
        />

</android.support.constraint.ConstraintLayout>